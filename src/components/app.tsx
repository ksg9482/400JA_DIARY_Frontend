import { LoggedInRouter } from '../routers/rogged-in-router'
import { LoggedOutRouter } from '../routers/rogged-out-router'
export const App = (isLoggedIn: boolean) => {
  return //isLoggedIn ? <LoggedInRouter /> : <LoggedOutRouter />
}